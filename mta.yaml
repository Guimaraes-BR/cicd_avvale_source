_schema-version: "3.2"
ID: poc-cicd
version: 1.0.0

modules:
  - name: poc-cicd-app
    type: nodejs
    path: .
    parameters:
      memory: 256M
      disk-quota: 256M
    build-parameters:
      builder: npm
