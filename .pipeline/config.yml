version: "1.0"

stages:
  Build:
    steps:
      script:
        name: "PoC - GitHub Source Pipeline"
        dockerImage: "python:3.11"
        script: |
          echo "Pipeline executado com sucesso no SAP CI&D"
          echo "Repositorio SOURCE clonado corretamente"
          echo "Branch: ${GIT_BRANCH}"
